{
  "name": "grounding-guard",
  "version": "0.1.0",
  "private": true,
  "description": "Evidence-grounded AI reliability platform",
  "scripts": {
    "dev": "echo Development commands will be configured",
    "build": "echo Build commands will be configured",
    "test": "echo Tests will be configured",
    "lint": "echo Lint commands will be configured"
  }
}
